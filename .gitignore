node_modules/

# Dados pessoais (fatos de perfil, currículos gerados) — nunca versionados.
# O app recria essas pastas sozinho na primeira vez que roda.
data/
output/

# Build do client (Vite gera tudo aqui via `npm run build`) — não versionar.
public/

# Chrome baixado pelo Puppeteer em produção (scripts/ensure-chrome.js).
.chrome-cache/

# Segredos e configuração de ambiente (chaves do Firebase, API keys, etc.).
.env
.env.*
*.local
serviceAccountKey.json
firebase-adminsdk*.json
server/firebase-service-account.json

# Sistema operacional / editor.
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
